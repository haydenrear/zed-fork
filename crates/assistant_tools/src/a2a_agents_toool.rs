// call the commit diff context MCP server, which then provides code context, embeds repositories, 
// and deploys code, then returns contextual information to be interpreted into code changes for this.

// one of the schema entries is the discovery URI, to other agents, and then each one provides options 